---
date: 2026-07-28
project: AxiomDSF
shipped: A second, fully mocked build of the console that runs with no backend at all.
outcome: shipped
note: >-
  Built so the interface could be evaluated without an API key, a server, or a
  credit balance. It turned into the version we actually demo, which was not the
  plan and is worth remembering next time.
---

Ten agents, six worked examples, both run modes, every output hardcoded. It
compiles to a single 882 kB HTML file that opens off the filesystem.
